<%= form_for(@link_tag) do |f| %>
  <% if @link_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_tag.errors.count, "error") %> prohibited this link_tag from being saved:</h2>

      <ul>
      <% @link_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link_id %><br>
    <%= f.number_field :link_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
