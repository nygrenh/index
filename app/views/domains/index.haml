%h1 Domains
%table.table.table-hover
  %thead
    %tr
      %th Domain
      %th Links
  %tbody
    - @domains.each do |domain|
      %tr
        %td= link_to domain.name, domain
        %td= domain.links_count
