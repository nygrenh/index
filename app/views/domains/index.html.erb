<h1>Domains</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Domain</th>
      <th>Links</th>
    </tr>
  </thead>

  <tbody>
    <% @domains.each do |domain| %>
      <tr>
        <td><%= link_to domain.domain, domain %></td>
        <td><%= domain.links.size %></td>
      </tr>
    <% end %>
  </tbody>
</table>
